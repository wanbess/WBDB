#include <iostream>
#include<algorithm>
#include "../include/main.h"
#include "../lib/inc/math.h"
#include"math.h"
int main(){
    Test a(2);
    #if defined(HAS_MAX)&&defined(HAS_MIN)
    std::cout<<"hello world "<(7- mul(3,a.Get()))  <<std::endl;
    #else  
    std::cout<<"hello world "<<std::endl;
    #endif
}